/** @type {import("prettier").Config} */
const config = {
  printWidth: 90,
  singleQuote: false,
  trailingComma: "none",
  semi: true,
  tabWidth: 2,
  plugins: ["prettier-plugin-tailwindcss"]
};

export default config;
